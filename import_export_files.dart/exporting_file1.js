const add = (a, b) => a + b;

export const sub = (a, b) => a - b;

const mul = (a, b) => a * b;

export const div = (a, b) => a / b;

export {add, mul};

export default sub;
export {default as greet} from "./exporting_file2.js";